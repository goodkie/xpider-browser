name = "xpider-monitor"
main = "worker.js"
compatibility_date = "2024-01-01"

# ── Cron Trigger: 매 1분마다 실행 ──────────────────────────
[triggers]
crons = ["* * * * *"]

# ── 환경 변수 (선택) ───────────────────────────────────────
# 수동 /run 엔드포인트 접근 시 사용할 시크릿 토큰
# wrangler secret put MONITOR_SECRET 으로 설정 가능
# 기본값: xpider-monitor-secret-2024
